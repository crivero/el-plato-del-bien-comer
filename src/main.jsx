import React from 'react';
import ReactDOM from 'react-dom/client';
import JuegoAlimentos from './index';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <JuegoAlimentos />
  </React.StrictMode>
);
